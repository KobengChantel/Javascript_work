  
 //Boolean value for an ID variable

  //Boolean value
    const id = true;
    //a ternary operator,message will check whether ID is valid
    const messages = (id) ? "Allowed In" : "Denied Entry";
    console.log(messages);
  